package:
  name: soma-workflow

soma-env:
  publication: neuro-forge
  components:
    - soma-workflow

  internal-dependencies:
    - soma-base

  type: interpreted

requirements:
  run:
    - six
    - pyzmq
    - paramiko
    - pyqt
    - matplotlib
